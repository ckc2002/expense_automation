<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Default Suite" parallel="methods" thread-count="3">

	<listeners>
        <listener class-name="listeners.RetryTransformer"/>
        <listener class-name="listeners.AllureListener"/>
    </listeners>

    <test name="Smoke Tests">
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>

		<!-- To Run the Whole Page and the test inside that -->
		<packages>
		 <package name="tests.smoke" /> 
			<package name="api.auth" />
		</packages> 
		
		
		
		<!-- To run separate test -->
       <!-- <classes>
            <class name="tests.smoke.LoginTest"/>
        </classes>-->
        
    </test>
    
</suite>